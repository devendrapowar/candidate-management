import { YearToMonthPipe } from './year-to-month.pipe';

describe('YearToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new YearToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
